# Порт, на якому запускається HTTP/Socket.IO сервер
PORT=4000

# Список origin'ів через кому, з яких дозволено CORS.
# У продакшні замініть на адресу свого frontend.
CORS_ORIGIN=http://localhost:5173
